[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so React Router handles all client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
